AVR:
Repo:  https://github.com/njaalsb/AVR_C/tree/main/USART

Funksjonalitet:
- 1 USART (Rx, Tx)
- 2 GPIO pins til LED-lys
- Mulighet for å sjekke et lagret byte

Pseudo-kode:

int main(void) {

if (sent == received) {
	LED = grønn;
} else {
	if (sent == saved) {
		LED = grønn;
        } else {
		LED = rød;
	}
}

}